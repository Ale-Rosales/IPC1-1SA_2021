class Doctor:

    def __init__(self, nDoctor, aDoctor, fDoctor, sDoctor, uDoctor, cDoctor, tDoctor):
        self.nDoctor = nDoctor
        self.aDoctor = aDoctor
        self.fDoctor = fDoctor
        self.sDoctor = sDoctor
        self.uDoctor = uDoctor
        self.cDoctor = cDoctor
        self.tDoctor = tDoctor
